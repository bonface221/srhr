/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "srhralliance.or.ke",
      "www.srhralliance.or.ke",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
